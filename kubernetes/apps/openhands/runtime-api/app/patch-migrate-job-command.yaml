apiVersion: batch/v1
kind: Job
metadata:
  name: openhands-runtime-api-migrate-db
spec:
  template:
    spec:
      containers:
        - name: migrate-db
          command:
            - "poetry"
            - "run"
            - "alembic"
            - "upgrade"
            - "head"
